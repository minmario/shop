<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="customer">
    <!-- 회원 가입 -->
    <insert id="signupInsert" parameterType="user.vo.CustomerVO">
        INSERT INTO customer (id, grade_code, cus_id, cus_pw, name, nickname, gender, birth_date, phone, email, is_del)
        VALUES (#{cus_id}, #{cus_pw}, #{name}, #{nickname}, #{gender}, #{birth_date}, #{phone}, #{email}, "0")
    </insert>

    <!-- 로그인 검증 -->
    <select id="loginSelect" parameterType="user.vo.CustomerVO" resultType="user.vo.CustomerVO">
        SELECT * FROM customer
        WHERE cus_id = #{cus_id} AND
        cus_pw = #{cus_pw}
    </select>

    <!-- 적립금 내역 가져오기 -->
    <select id="select_point" resultType="user.vo.PointVO" parameterType="String">
        SELECT * FROM point
        WHERE cus_no = #{cus_no};
    </select>
</mapper>