<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="seller">
    <select id="login" parameterType="String" resultType="comm.vo.SellerVO">
        SELECT * FROM seller
        WHERE seller_id = #{seller_id}
        AND active=0
    </select>
    <select id="getSellerInfo" resultType="comm.vo.SellerVO">
        SELECT seller_id, name, phone, email, `desc`, address, seller_icon FROM shop.seller where id = 2
    </select>

    <update id="updateSellerInfo">
        update shop.seller set seller_id = #{seller_id}, name = #{name}, phone = #{phone}, email = #{email}, `desc` = #{desc}, address = #{address},
        seller_icon = #{seller_icon} where id = 2
    </update>
    <update id="updateSellerIcon">
        UPDATE shop.seller
        SET seller_icon = #{seller_icon}
        WHERE seller_id = 2
    </update>
    <select id="check_id" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM seller
        WHERE seller_id = #{seller_id}
    </select>
    <insert id="add_seller" parameterType="Map">
        INSERT INTO seller(seller_id,seller_pw,email,phone,name,address,courier,`desc`)
        VALUES(#{seller_id},#{seller_pw},#{email},#{phone},#{name},#{address},#{courier},#{desc})
    </insert>
</mapper>