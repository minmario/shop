<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="delivery">
    <!-- 인자로 카테고리 값을 받아 검색하는 기능 -->

    <select id="deliverySelect" parameterType="user.vo.DeliveryVO" resultType="user.vo.DeliveryVO">

    </select>
</mapper>