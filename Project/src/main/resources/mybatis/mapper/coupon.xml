<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="coupon">
    <!-- 상품에 적용할 수 있는 쿠폰 목록 -->
    <select id="select_prod_coupon" resultType="user.vo.CouponVO" parameterType="Map">
        SELECT c.*
        FROM coupon c
        JOIN product p ON (p.category_no = c.category_no OR c.type = 1)
        WHERE p.id = #{prod_no} AND c.grade_no = #{grade_no} AND c.status = '1' AND c.is_del = '0'
    </select>

    <!-- 쿠폰 사용 이력 추가 -->
    <insert id="insert_cus_coupon" parameterType="Map">
        INSERT INTO cus_coupon (cus_no, coupon_no, order_code, used_date, status)
        VALUES (#{cus_no}, #{coupon_no}, #{order_code}, NOW(), 2)
    </insert>
</mapper>
