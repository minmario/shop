<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="order">
  <select id="new_order_list" parameterType="String" resultType="comm.vo.OrderVO">
    SELECT product.*,`order`.*, customer.cus_id,inventory.option_name
    FROM `order`
    LEFT JOIN product ON `order`.prod_no = product.id
    LEFT JOIN customer ON customer.id = `order`.cus_no
    LEFT JOIN inventory ON `order`.inventory_no = inventory.id
    WHERE product.seller_no = #{seller_no}
    AND `order`.status = 0
  </select>
</mapper>
